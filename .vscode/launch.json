{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "메인디버그",
            "type": "python",
            "request": "launch",
            "program": "${workspaceFolder}/main.py",
            "console": "integratedTerminal",
            "cwd": "${workspaceFolder}"
        }
    ]
}